Cloudflare Workers & Pages 靜態部署版
1. 將本 ZIP 解壓縮。
2. 在 Cloudflare「Upload static files to update your Worker」選擇解壓後的所有檔案/資料夾。
3. 確認 index.html 位於最外層後按 Deploy。
4. 瀏覽器第一次錄音會詢問麥克風權限，請選「允許」。

功能：18張畫面依序播放、背景音樂、按鍵音效、瀏覽器錄音 Demo、最後獎勵頁新增「重新挑戰」按鍵，按下回到第1張開始頁。
注意：本版錄音為 Demo 流程，尚未串接客委會語音辨識 API；企畫書定義 API 應在後端處理辨識。
